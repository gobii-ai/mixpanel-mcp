{
  "name": "@mattgreathouse/mixpanel-mcp",
  "version": "1.0.2",
  "description": "MCP server for querying Mixpanel data",
  "main": "build/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc && node -e \"require('fs').chmodSync('build/index.js', '755')\"",
    "prepack": "npm run build"
  },
  "keywords": [
    "mcp",
    "mixpanel",
    "model-context-protocol"
  ],
  "author": "Matt Greathouse",
  "license": "ISC",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.6.1",
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "@types/node": "^22.13.5",
    "typescript": "^5.8.2"
  },
  "type": "module",
  "bin": {
    "mixpanel-mcp": "./build/index.js"
  },
  "files": [
    "build"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/gobii-ai/mixpanel-mcp.git"
  },
  "bugs": {
    "url": "https://github.com/gobii-ai/mixpanel-mcp/issues"
  },
  "homepage": "https://github.com/gobii-ai/mixpanel-mcp#readme",
  "publishConfig": {
    "access": "public"
  }
}
