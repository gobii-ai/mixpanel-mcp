# Smithery configuration file: https://smithery.ai/docs/config#smitheryyaml

startCommand:
  type: stdio
  configSchema:
    type: object
    additionalProperties: false
  commandFunction:
    |-
    () => ({
      command: 'node',
      args: ['build/index.js']
    })
